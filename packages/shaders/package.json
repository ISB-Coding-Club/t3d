{
    "name": "@t3d-engine/shaders",
    "description": "The T3D engine's built-in shaders.",
    "version": "0.1.0-beta.2",
    "main": "dist/index.js",
    "scripts": {
        "build": "ts-node esbuild.config.ts",
        "watch": "ts-node esbuild.watch.ts",
        "clean": "rimraf dist"
    },
    "dependencies": {
        "@babylonjs/core": "^5.32.1",
        "@t3d-engine/util": "workspace:*"
    },
    "devDependencies": {
        "concurrently": "^7.5.0",
        "esbuild": "^0.15.13"
    }
}
